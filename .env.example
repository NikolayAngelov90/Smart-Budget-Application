# ==============================================================================
# Smart Budget Application - Environment Variables Template
# ==============================================================================
# Copy this file to .env.local and fill in your actual values
# IMPORTANT: Never commit .env.local to version control
# ==============================================================================

# ------------------------------------------------------------------------------
# Supabase Configuration (Required)
# ------------------------------------------------------------------------------
# Get these from: https://app.supabase.com/project/_/settings/api

NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# ------------------------------------------------------------------------------
# Google OAuth (Required for Social Login)
# ------------------------------------------------------------------------------
# Get these from: https://console.cloud.google.com/apis/credentials

GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# ------------------------------------------------------------------------------
# GitHub OAuth (Required for Social Login)
# ------------------------------------------------------------------------------
# Get these from: https://github.com/settings/developers

GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# ------------------------------------------------------------------------------
# Vercel Cron Job Authentication (Required for Production)
# ------------------------------------------------------------------------------
# Generate a secure random string (minimum 32 characters)
# Example: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

CRON_SECRET=your_secure_cron_secret_minimum_32_characters

# ------------------------------------------------------------------------------
# Redis Configuration (Required for Production - Optional for Local Development)
# ------------------------------------------------------------------------------
# Option 1: Upstash Redis (Recommended for Vercel/Serverless)
# Get these from: https://console.upstash.com/
UPSTASH_REDIS_REST_URL=your_upstash_redis_rest_url
UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_rest_token

# Option 2: Self-hosted Redis (Alternative to Upstash)
# Use this for traditional Redis instances (Docker, Redis Cloud, etc.)
# REDIS_URL=redis://localhost:6379
# REDIS_URL=redis://username:password@your-redis-server:6379

# Feature Flag: Enable/Disable Redis Rate Limiting
# Set to 'false' to disable Redis and use in-memory fallback
# Default: true (Redis enabled)
# USE_REDIS_RATE_LIMIT=true

# Used for: Rate limiting across multiple serverless instances
# Fallback: In-memory Map (local development only, not production-ready)

# ------------------------------------------------------------------------------
# Application URL (Required for Production)
# ------------------------------------------------------------------------------
# Your deployed application URL (e.g., https://your-app.vercel.app)
# Used for OAuth callbacks and email links

NEXT_PUBLIC_APP_URL=http://localhost:3001

# ==============================================================================
# Environment Setup Instructions
# ==============================================================================
#
# 1. Local Development:
#    - Copy this file to .env.local
#    - Fill in Supabase credentials (required)
#    - Fill in OAuth credentials (required for social login testing)
#    - CRON_SECRET can be any string for local testing
#    - Redis credentials are optional (will use in-memory fallback)
#    - NEXT_PUBLIC_APP_URL should be http://localhost:3001
#
# 2. Vercel Production Deployment:
#    - Add all variables in Vercel Dashboard → Settings → Environment Variables
#    - Select "Production" environment
#    - Redeploy after adding new variables
#    - Redis credentials are REQUIRED for production (multi-instance support)
#
# 3. Vercel Preview Deployments:
#    - Same variables as production
#    - Select "Preview" environment in Vercel dashboard
#    - Can use same values as production or separate preview database
#
# ==============================================================================
# Security Notes
# ==============================================================================
#
# - NEVER commit .env.local to git (already in .gitignore)
# - Rotate secrets regularly (especially CRON_SECRET and OAuth secrets)
# - Use different values for production and preview environments
# - Store production secrets in secure password manager
# - Limit access to Vercel project environment variables
#
# ==============================================================================
